# DURF Werewolf Simulation

Main experiment setting:

- Unless otherwise stated, experiments are run with 500 games per condition.
- A fixed random seed is used for reproducibility.
- Smaller 100-game runs are used only for quick debugging and should not be used as final reported results.
